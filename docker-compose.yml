services:
  docusaurus:
    image: cloudseidoranalytics/portal:14a3e509f2b75323fa53b4ae9a00bf92cbf4cdb9
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped