services:
  docusaurus:
    image: cloudseidoranalytics/portal:e090a07ad42ed5570a319be6bb8d72ab5db0f232
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped