services:
  docusaurus:
    image: cloudseidoranalytics/portal:c0cbe1ab2b3c74797f137ece0126ccdea91719b7
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped