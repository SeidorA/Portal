services:
  docusaurus:
    image: cloudseidoranalytics/portal:7c8a888c1e0d356ec1e14dd2e499cfa70cd9aa0b
    # IMPORTANTE: No añadir sección 'build' aquí. 
    # La imagen se construye en GitHub Actions y Portainer solo debe descargarla (PULL).
    ports:
      - "3050:3050"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped
