services:
  docusaurus:
    image: cloudseidoranalytics/portal:6cd54bd1e27026dac08f9385e8b563b2c498858f
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped