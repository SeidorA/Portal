services:
  docusaurus:
    image: cloudseidoranalytics/portal:d36e817c3b7bd13769ac55624f8f291f1f5afe88
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped