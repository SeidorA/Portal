services:
  docusaurus:
    image: cloudseidoranalytics/portal:458f4ef99041b6770cfe3d455395f4272e5e3937
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped