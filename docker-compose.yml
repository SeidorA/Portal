services:
  docusaurus:
    image: cloudseidoranalytics/portal:aed00795d47c5eea48865494415b432e739f2d5e
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped