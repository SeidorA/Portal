services:
  docusaurus:
    image: cloudseidoranalytics/portal:latest
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped