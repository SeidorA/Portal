services:
  docusaurus:
    image: cloudseidoranalytics/portal:6561cd529f511b302dc58911b3805b05640286e1
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped