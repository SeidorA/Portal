services:
  docusaurus:
    image: cloudseidoranalytics/portal:75cf2126bdcf2d1086faf5cbe71aa5e48656c7b4
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped