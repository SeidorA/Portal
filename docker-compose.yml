services:
  docusaurus:
    image: cloudseidoranalytics/portal:bc144d27accf90fd06042ac9dee54678314082b6
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped