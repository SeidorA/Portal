services:
  docusaurus:
    image: cloudseidoranalytics/portal:7377acb86052817f5adae05de12a3b871f37e4cb
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped