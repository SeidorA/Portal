services:
  docusaurus:
    image: cloudseidoranalytics/portal:036d21fe6e17c4804375f1cd9eedeb222eb96692
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped