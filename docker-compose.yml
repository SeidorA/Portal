services:
  docusaurus:
    image: cloudseidoranalytics/portal:5228b98072851ef65b8765ff73dac5a1e20e9ce3
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped