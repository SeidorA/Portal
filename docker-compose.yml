services:
  docusaurus:
    image: cloudseidoranalytics/portal:f90ac19c81aa37f8df75e96f794f36c9a7f4c5e0
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped