services:
  docusaurus:
    image: cloudseidoranalytics/portal:e4ded9788439dcc7bd5302889031961f4a2addcd
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped