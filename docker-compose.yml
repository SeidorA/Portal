services:
  docusaurus:
    image: cloudseidoranalytics/portal:c05a26eef0b410ebb458d845abdfd802b1033e6c
    build:
      context: .
    ports:
      - "3050:80"  # Solo HTTP, Nginx Proxy Manager hace el TLS
    restart: unless-stopped